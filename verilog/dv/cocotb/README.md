# Cocotb Tests

The active project test is user_proj_tests/x1_memory_compiler_noc.

It directly drives the X1 compiler CSRs inside the Caravel wrapper, verifies X1 behavioral program/readback, and checks the FP4LLM-style NoC header and packed payload values.

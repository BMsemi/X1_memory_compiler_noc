# User Project Cocotb Tests

Active test list:

- x1_memory_compiler_noc: drives the X1 memory compiler CSRs inside Caravel, verifies X1 behavioral program/readback, and checks the FP4LLM-style SAR/TDC NoC header and packed payload.

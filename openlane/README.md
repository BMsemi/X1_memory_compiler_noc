# OpenLane Notes

This repository currently packages the X1 memory compiler as behavioral RTL plus Caravel cocotb verification.

The inherited SNN OpenLane configuration and generated physical views were removed because they do not match the new X1-only wrapper. Add a fresh openlane/user_project_wrapper/config.json when the compiler wrapper is ready to harden.
